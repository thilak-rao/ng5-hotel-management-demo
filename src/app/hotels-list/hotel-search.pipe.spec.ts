import { HotelSearchPipe } from './hotel-search.pipe';

describe('HotelSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new HotelSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
